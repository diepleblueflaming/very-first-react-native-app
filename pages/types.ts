import { ImageURISource } from "react-native";

export type PropsTypes = {
  url: ImageURISource,
  name: string,
  alsoKnowAs: string,
  homepage: string,
  knownfordepartment: string,
  dateofbirth: string,
  biography: string,
  deathday: string
}
